# This module has been split into three focused modules:
#   _redact_mixin.py   — redaction search, term management, preview, apply
#   _profiles_mixin.py — censorship profile dialogs
#   _agent_mixin.py    — AI document-analysis agent panel and chat
#
# viewer.py now imports _RedactMixin, _ProfilesMixin and _AgentMixin directly.
